/**
 * Platform-independent BabyCare Companion core entry point.
 *
 * Platform adapters (such as Scriptable or an ICS exporter) belong outside this
 * module so planning data and business rules remain portable.
 */
export const projectVersion = "0.1.0";
